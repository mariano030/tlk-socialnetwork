<REgistraiton />

<HashRouter>
    <Route exact path="/" component={ Registraiton} />
    <Route path="/login" component={Login}/>
    <Route path="/reset" component={ResetPassword} />
</HashRouter>


// transition between displays reset, verify, complete

{/* 
also send the email address to server, still in state, when you  */}


email VARCHAR NOT NULL REFERENCES users(email),
code....


let crs = require("crypto-random-string");

// only the most recent code - sort by created_at 

</div>